# 项目名称（与 GitHub 仓库名一致）
name = "otp-extract-cloudflare"

# Worker 入口文件
main = "src/index.ts"

# 兼容性日期（使用日志中检测到的最新日期）
compatibility_date = "2025-11-11"

# 启用 Node.js 兼容性模式（必须保留）
compatibility_flags = ["nodejs_compat"]

# 开发环境配置（本地调试用，不影响部署）
[dev]
port = 8787
